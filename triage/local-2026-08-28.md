# Triage — run `local-2026-08-28`

- Report: `reports/last-run.json` (Playwright JSON reporter)
- Suite start: 2026-08-29T02:42:04.843Z · duration 46.5s
- Totals: 34 executions — 33 expected, 1 unexpected, 0 flaky, 0 skipped
- Target: `vizaeo` @ `https://staging.vizaeo.com` (baseURL from `playwright.config.ts`)
- Flake tracker: `uv run python scripts/flake_tracker.py reports/last-run.json local-2026-08-28`
  → **no output** (zero tests at/over the 3-of-last-10 threshold). `flake-history.json`
  created with 34 entries, every one `"flaky": false`. This is run 1 of the window,
  so no test can yet cross the threshold; the file is the baseline.

---

## Failure 1 — `seed-failure.spec.ts > @seed-failure heading that does not exist > chromium`

**Verdict: script-issue (deliberate — validation scaffolding, not a real defect)**

### What the run says

Failed on attempt 0 (6.09s) and again on retry 1 (5.78s) with the identical error:

```
expect(locator).toBeVisible() failed
Locator: getByRole('heading', { name: 'Testah Seed Failure Heading' })
Expected: visible / Timeout: 3000ms / Error: element(s) not found
```

Deterministic across both attempts → **not a flake** (a flake would have gone green
on retry and been recorded as `flaky` by the tracker; it was not).

### Evidence chain

1. **The spec declares itself.** `tests/specs/seed-failure.spec.ts` header:
   > `@seed-failure — deliberate failure to exercise the triage path during loop
   > validation (docs/plans/2026-08-28-testah-v1.md Task 13). DELETE after the
   > validation review. Exempt from POM/traceability as validation scaffolding.`
   `RULES.md` sanctions the `@seed-failure` tag for exactly this: "deliberate
   failures for loop validation only." The string `Testah Seed Failure Heading`
   is invented — it has never existed in the product.

2. **Trace/error-context snapshot** (`test-results/seed-failure--seed-failure-heading-that-does-not-exist-chromium-retry1/`,
   trace at `trace.zip`) shows a fully rendered page at the moment of failure:
   `heading "Welcome to Vizaeo" [level=1]`, an Email textbox, `Continue`, and
   Google/Apple OAuth buttons. The page loaded fine; only the fictional heading
   was absent.

3. **Live replay (Playwright MCP, 2026-08-29T02:44Z)** — `goto https://staging.vizaeo.com/`:
   - Lands on `https://staging.vizaeo.com/login`, title `Sign in · Vizaeo`.
   - Accessibility snapshot matches the trace exactly: single `<h1>` = "Welcome to
     Vizaeo", email-first Clerk sign-in with Google/Apple. No "Testah Seed Failure
     Heading" anywhere.
   - **Console: zero JS exceptions.** Warnings only (PostHog key absent on staging;
     Clerk development-keys notice; Clerk `afterSignInUrl` deprecation).
   - **Network: no product API 4xx/5xx.** Clerk `/v1/environment` 200, `/v1/client`
     200, RSC fetches for `/` 200, telemetry 200. The only errors are two missing
     static assets — `GET /favicon.ico` 404 and `GET /icon-192.png` 404 (the latter
     also trips a manifest-icon warning). Cosmetic, unrelated to the assertion.

4. **The redirect is correct, not a symptom.** `targets.yaml` documents that `/` is
   Clerk-gated on staging (307 → login) and deliberately excludes it from the
   designated pages. The behaviour the seed spec incidentally exercises is already
   covered and **passing**: `smoke.spec.ts > @smoke unauthenticated root redirects
   to sign-in`. So the site did the right thing on the one behaviour under the
   navigation; the assertion on top of it was fictional.

### Criterion violated

**None — and that absence is itself part of the verdict.** `requirements/` contains
only `vizaeo/pricing/{primary-cta-route,faq-accordion}.md` and
`vizaeo/help/{card-grid,anonymous-access}.md`. There is no approved criterion for
`/` (or for any heading on it), and this spec carries no `// implements:` header
because it is exempt scaffolding. A product-bug verdict requires the site to
contradict an approved criterion; with no criterion to contradict, product-bug is
unavailable on the evidence alone. Combined with the live replay showing a healthy,
correctly-gated page, the assertion — not the product — is what is wrong. That is
the definition of script-issue.

Corollary worth stating: `/` is not in `targets.yaml`, so there is no
`page-maps/vizaeo/home/` and therefore **no selector provenance** for this locator.
Any non-scaffolding spec asserting against `/` would violate RULES.md ("selectors
come from the page-map") before it ever ran.

### Drift correlation

**None.** `changed-pages.json` is `[]` (empty mailbox — Scout reported no drift).
`page-maps/` has not changed since `3e7cc3d scout: first page maps for vizaeo /help
and /pricing`, and covers only `help` and `pricing` — neither is touched by this
spec. The failure was introduced by `52d9362 test: plant @seed-failure spec for loop
validation triage`, the immediately preceding commit, and by nothing on the target.

### Recommended next action

1. **No product ticket. No bug filed.** Nothing here is a defect.
2. **Delete `tests/specs/seed-failure.spec.ts`** once this validation review is
   accepted — the spec's own header instructs exactly that. It is the only
   outstanding action.
3. **No Steward escalation** for flakiness: zero threshold crossings this run.
4. *(Observation, out of scope — no criterion, no ticket requested)*: staging serves
   404 for `/favicon.ico` and `/icon-192.png`. Surfacing it here only because the
   replay saw it; it is a candidate for a `targets.yaml`/criteria conversation, not
   a triage outcome.

---

## Run summary

| Spec file | Executions | Passed | Failed | Flaky | Verdict |
| --- | ---: | ---: | ---: | ---: | --- |
| `help-anonymous-access.spec.ts` | 12 | 12 | 0 | 0 | clean |
| `help-card-grid.spec.ts` | 11 | 11 | 0 | 0 | clean |
| `pricing-faq-accordion.spec.ts` | 6 | 6 | 0 | 0 | clean |
| `pricing-primary-cta.spec.ts` | 2 | 2 | 0 | 0 | clean |
| `smoke.spec.ts` | 2 | 2 | 0 | 0 | clean |
| `seed-failure.spec.ts` | 1 | 0 | 1 | 0 | **script-issue** (planted `@seed-failure`) |
| **Total** | **34** | **33** | **1** | **0** | 1 failure, fully explained |

| Verdict | Count | Tests |
| --- | ---: | --- |
| product-bug | 0 | — |
| script-issue | 1 | `seed-failure.spec.ts > @seed-failure heading that does not exist > chromium` |
| flake | 0 | — |

**Bottom line:** 33 of 34 executions passed. The single failure is the planted
`@seed-failure` spec, reproduced deterministically across both attempts and
confirmed live: staging is healthy, correctly redirects `/` → `/login`, throws no
console exceptions, and returns no failing product API calls. Zero flakes recorded;
`flake-history.json` now holds the baseline window for all 34 test ids. The triage
path works end to end — the seed can be removed.
