---
name: triage
description: Run an Author pass (Mode B) — triage the latest test run and refresh the coverage maps
argument-hint: <run-id>
disable-model-invocation: true
---

Read `agents/author.md` in full and follow **Mode B (Triage)**. Run-id:
`$ARGUMENTS` (no argument = `local-<today's ISO date>` for a local run). Run
from the repo root.
