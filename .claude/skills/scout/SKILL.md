---
name: scout
description: Run a Scout pass — crawl, judge, drift-check, and flag the designated pages of a target
argument-hint: <target>
disable-model-invocation: true
---

Read `agents/scout.md` in full and follow it for target `$ARGUMENTS`
(no argument = every target in `targets.yaml`). Run every command from the
repo root.
