---
name: author
description: Run an Author pass (Mode A) — criteria, POMs, specs, reviewer checkpoint
argument-hint: <target>
disable-model-invocation: true
---

Read `agents/author.md` in full and follow **Mode A (Authoring)** for target
`$ARGUMENTS`. Stay in the main thread — the Reviewer checkpoint is a spawn of
the `reviewer` agent type via the Agent tool. Run from the repo root.
